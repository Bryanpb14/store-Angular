export interface Product {

    id: string;
    name: string;
    title: string;
    price: number;
    description: string;
    image: string;
}

//se usa cuando tengo un obejto, (arreglos de datos) con diferentes tipos
